import DisorderHistory from "../../features/disorderHistory/DisorderHistory";

/**
 * Disorders Component
 *
 * This component serves as a wrapper for the DisorderHistory feature.
 * It encapsulates the rendering of the DisorderHistory component.
 *
 * @component
 * @returns {JSX.Element} - The rendered component.
 */
const Disorders = () => {
  return <DisorderHistory />;
};

export default Disorders;
