import * as states from "./dashboard/states";
import * as pingStatuses from "./pingStatus";
import * as info from "./dashboard/info";
import * as history from "./dashboard/history";

const api = { states, info, history, pingStatuses };

export default api;
